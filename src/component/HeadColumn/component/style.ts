import styled from "styled-components";

export const PerferenceCerterDropdown = styled.div`
    display: flex;
    color: var(--color, black);
    font-size: 14px;
    margin: 10px 18px;
    position: relative;
    top: 1px;
`

export const PerferenceCerterAvator = styled.img`
  width: 18px;
  height: 18px;
`;

export const PerferenceCenterContainer = styled.div``